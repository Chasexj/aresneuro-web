# aresneuro.com

Landing site for Ares Neuro. React + Vite + Tailwind, deployed to GitHub Pages via `.github/workflows/deploy.yml` on every push to `main`.

## Local development

```bash
npm install
npm run dev      # http://localhost:3000
npm run build    # outputs to dist/
```
